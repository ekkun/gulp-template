<%_
  const state = process.env.state;
  if (state === 'prod') {
    // 本番環境
    domain = 'https://prod.com';
    path = '/';
    dir = '/';
  } else if (state === 'dev') {
    // 開発環境
    domain = 'https://dev.com/sub-directory';
    path = '/sub-directory/';
    dir = '/sub-directory/';
  } else {
    // ローカル環境
    domain = 'http://localhost:4000';
    path = '/';
    dir = '/';
}
_%>
